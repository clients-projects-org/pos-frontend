import { IImageSizeInfoType } from './index';

const userStoreImageInfo: IImageSizeInfoType = {
	size: 500,
	height: 1000,
	width: 1000,
	viewWidth: 'w-[70px]',
	viewHeight: 'h-[70px]',
};

export { userStoreImageInfo };
